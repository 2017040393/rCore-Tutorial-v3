[package]
name = "os"
version = "0.1.0"
authors = ["Yifan Wu <shinbokuow@163.com>"]
edition = "2024"

# 添加这个配置来禁用标准测试框架
[[bin]]
name = "os"
test = false
bench = false

[dependencies]
log = "0.4"
sbi-rt = { version = "0.0.3", features = ["legacy"] }

[profile.release]
debug = true
